import React from 'react';
//import homeStyle from './homeStyle';

const Footer = ({ appName }) => {
  return (
    <footer>
        <h1>{appName}</h1>
        <p>FOOTER</p>
    </footer>
  );
};

export default Footer;
